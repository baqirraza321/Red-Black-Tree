# Red-Black-Tree
C++ implementation of a Red-Black Tree without parent pointers. Supports insertion, deletion, search, traversals, rotations, recoloring, and console visualization. Parent nodes are located dynamically using recursive traversal, demonstrating an alternative self-balancing BST implementation.
